---
# Replaces Kawanagare

source_files: [../src/electricLine.cpp]
hooks:
  - name: electricLineBuild
    type: add_func_pointer
    src_addr_pal: 0x809622F8
    target_func: 'daElectricLine::build()'

